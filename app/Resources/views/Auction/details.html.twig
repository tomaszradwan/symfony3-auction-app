{% extends "base.html.twig" %}

{% block body %}
    <h1>{{ auction.title }}</h1>
    <p>{{ auction.description }}</p>
    <p>{{ auction.price }}</p>
    <a href="{{ url('auction_index') }}">Back to main page</a>
{% endblock %}
